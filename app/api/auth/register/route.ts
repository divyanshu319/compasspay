import { Prisma } from "@prisma/client";
import { NextRequest, NextResponse } from "next/server";
import bcrypt from "bcryptjs"; import { prisma } from "@/lib/prisma"; import { authSchema } from "@/lib/validation"; import { assertSessionConfiguration, createSession, sessionCookie } from "@/lib/auth";
export async function POST(req: NextRequest) { try { const parsed = authSchema.safeParse(await req.json()); if (!parsed.success) return NextResponse.json({ error: "Please provide a valid name, email, and 8+ character password." }, { status: 400 }); assertSessionConfiguration(); const { name, email, password } = parsed.data; const user = await prisma.user.create({ data: { name, email, passwordHash: await bcrypt.hash(password, 12) } }); const res = NextResponse.json({ data: { id: user.id, name, email } }, { status: 201 }); res.cookies.set(sessionCookie(await createSession(user))); return res; } catch (error) { if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === "P2002") return NextResponse.json({ error: "An account already exists for this email." }, { status: 409 }); console.error("Registration failed", error); return NextResponse.json({ error: "Unable to create account." }, { status: 500 }); } }
