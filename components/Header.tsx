import Link from "next/link";
import { currentUser } from "@/lib/auth";
import { LogoutButton } from "@/components/LogoutButton";
export async function Header() { const user = await currentUser(); return <header className="border-b border-[#dce5dc] bg-[#f7f7f2]/95"><nav className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><Link href="/" className="text-xl font-black tracking-tight">compass<span className="text-moss">pay</span></Link><div className="flex items-center gap-3 text-sm font-semibold"><Link href="/companies" className="hidden sm:block">Companies</Link><Link href="/compare" className="hidden sm:block">Compare</Link>{user ? <><span className="hidden max-w-40 truncate text-[#52705e] sm:block" title={user.email}>Hi, {user.name}</span><LogoutButton /></> : <><Link href="/sign-in" className="hidden sm:block">Sign in</Link><Link href="/sign-up" className="hidden sm:block">Create account</Link></>}<Link href="/submit" className="button text-sm">Add compensation</Link></div></nav></header>; }
