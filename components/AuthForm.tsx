"use client";
import { useState } from "react"; import Link from "next/link";
export function AuthForm({mode}:{mode:'login'|'register'}){const [error,setError]=useState(''),[loading,setLoading]=useState(false);const signup=mode==='register';async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const d=await r.json();setLoading(false);if(r.ok)window.location.assign('/submit');else setError(d.error||'Unable to continue.');}return <main className="mx-auto max-w-md px-5 py-16"><h1 className="text-4xl font-black">{signup?'Create account':'Welcome back'}</h1><p className="mt-2 text-gray-600">{signup?'Save and contribute transparent compensation data.':'Sign in to contribute compensation data.'}</p><form className="card mt-7 grid gap-5 p-6" onSubmit={submit}>{signup&&<label><span className="mb-1.5 block text-sm font-bold">Name</span><input name="name" required /></label>}<label><span className="mb-1.5 block text-sm font-bold">Email</span><input name="email" type="email" required /></label><label><span className="mb-1.5 block text-sm font-bold">Password</span><input name="password" type="password" minLength={8} required /></label><button className="button" disabled={loading}>{loading?'Please wait…':signup?'Create account':'Sign in'}</button>{error&&<p className="text-sm font-semibold text-red-700">{error}</p>}</form><p className="mt-5 text-sm">{signup?'Already have an account?':'New here?'} <Link className="font-bold underline" href={signup?'/sign-in':'/sign-up'}>{signup?'Sign in':'Create one'}</Link></p></main>}
